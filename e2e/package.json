{
  "name": "tithe-e2e",
  "private": true,
  "description": "Playwright UI tests / screenshots for the Tithe web front end. Lets the agent drive and *see* the canvas viewer (which is validated by eye, not review).",
  "scripts": {
    "test": "playwright test",
    "shots": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
